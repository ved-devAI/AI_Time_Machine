# Optional: copy to .env only when testing the live Responses API path.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6
# The default is artifact and never makes a paid runtime request.
AI_TIME_MACHINE_ANALYSIS_MODE=artifact
